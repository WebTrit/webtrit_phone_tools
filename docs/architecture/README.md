# Architecture

Why this CLI is shaped the way it is, and what a build inherits by checking out
one revision of it rather than another.

| Page | What it holds |
| --- | --- |
| [backend-address.md](backend-address.md) | Which backend a build talks to, why the revision decides it, and why an address override is not enough |

Belongs here: a shape that outlives a command. What does not: the options of a
command ([reference/](../reference/README.md)).
