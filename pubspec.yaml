name: webtrit_phone_tools
description: WebTrit Phone CLI tools
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  cli_completion: ^0.5.0
  mason_logger: ^0.2.12
  path: ^1.9.0
  pub_updater: ^0.4.0
  package_rename: ^1.5.0
  simple_mustache: ^2.1.0
  http: ^1.1.0

  dto:
    path: ../webtrit_phone_configurator_common/packages/dto

dev_dependencies:
  build_runner: ^2.4.8
  build_verify: ^3.1.0
  build_version: ^2.1.1
  mocktail: ^1.0.3
  test: ^1.25.2
  webtrit_analysis:
    git: https://github.com/WebTrit/webtrit_analysis.git

executables:
  webtrit_phone_tools:
